# Distpicker

##使用方法  
```
var dispicker = new dispicker();
  dispicker.init({
    elProvince: ".province-area",//省份类名（需绑定在input上面）
    elCity: ".city-area",//城市类名（同上）
    province: "河南省",//初始化赋值（可不传）
    city: "郑州市"//同上
  });
```
